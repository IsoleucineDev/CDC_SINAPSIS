
  # Congreso de Ciencias Sinapsis

  This is a code bundle for Congreso de Ciencias Sinapsis. 

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  # CDC_SINAPSIS
